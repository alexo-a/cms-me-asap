<article class="post">
    <div class="title">
        {{!--<a href="/dashboard/edit/{{id}}">{{title}}</a>--}}
        <a href="/post/{{id}}">{{title}}</a>
        <span class="posted-by">Posted by {{user.username}} on {{format_date created_at}}</span>
        
    </div>
    <div class="meta">
        {{post_body}}
    </div>
</article>